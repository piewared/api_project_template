# Environment Configuration
# Controls application behavior (development/production/test)
ENVIRONMENT=development

# Logging level (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO

# Infrastructure URLs - Sensitive connection strings that should be environment-specific
DATABASE_URL=sqlite:///./database.db
REDIS_URL=redis://localhost:6379/0
BASE_URL=http://localhost:8000

# Secrets - These MUST be configured in production
SECRET_KEY=your-secret-key-here-256-bits
JWT_SECRET=your-jwt-secret-here

### OIDC Configuration ###

# Global OIDC redirect URI (used by all providers unless overridden)
OIDC_REDIRECT_URI=http://localhost:8000/web/callback

# Note: OIDC provider configuration (endpoints, scopes, etc.) 
# should be defined in oidc-providers.yaml, not here. But client IDs/secrets for each provider
# can be set via environment variables if desired, e.g.:

# Example Google OIDC Provider
#OIDC_GOOGLE_CLIENT_ID=your-google-client-id
#OIDC_GOOGLE_CLIENT_SECRET=your-google-client-secret

# Example Custom OIDC Provider (example - define in oidc-providers.yaml first)
# OIDC_MY_PROVIDER_CLIENT_ID=your-custom-provider-client-id
# OIDC_MY_PROVIDER_CLIENT_SECRET=your-custom-provider-client-secret

